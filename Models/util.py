def data_process(file_path):
    # 1. open download files
    # 2. store all the content, delete the bad data
    # 3. for each stocks, generate moving averages and signals for each day (maybe store in mysql)
    # 4. return a complete data list
